<div class="content-title">
  <h2>@yield('table')</h2>
  <p>@yield('summary_table')</p>
</div>
<div class="button-add-new">
  <a class="btn btn-primary" href="@yield('link')">
    <span>Tạo mới</span>
    <small>@yield('table')</small>
  </a>
</div>
